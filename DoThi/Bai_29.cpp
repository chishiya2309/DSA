#include <bits/stdc++.h>

using namespace std;

int n, s, t, u, v, a[1003][1003];

int dx[] = {-1, 1, 0, 0};
int dy[] = {0, 0, -1, 1};

int bfs(pair<int, int> start, pair<int, int> end)
{
    queue<pair<pair<int, int>, int>>q;
    q.push({start, 0});
}

int bfs()

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    


    return 0;
}